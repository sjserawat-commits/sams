"use client";
import Link from "next/link";
import { Suspense, useEffect, useMemo, useState } from "react";
import { useSearchParams } from "next/navigation";

type Patient={id:number;patientId:string;firstName:string;lastName:string;phone?:string|null;gender?:string|null;dateOfBirth?:string|null};
type Investigation={id:number;code:string;name:string;category:string;department?:string|null;rate:number;specimen?:string|null};
type Mode="REGISTERED"|"CONSULTATION"|"WALKIN"|"EXTERNAL_REFERRAL";
const name=(p:Patient)=>`${p.firstName||""} ${p.lastName||""}`.trim()||"Patient";
const modes:[Mode,string,string,string][]=[["REGISTERED","Registered Patient","Search registered records","01"],["CONSULTATION","Consultation / Visit","Link to an existing Visit","02"],["WALKIN","Walk-in / Outsider","Enter patient information","03"],["EXTERNAL_REFERRAL","External Referral","Referral-based order","04"]];
const visitDateValue=(v:any)=>v?.visitDate||v?.date||v?.createdAt||v?.updatedAt||null;
const visitTimestamp=(v:any)=>{const raw=visitDateValue(v);const t=raw?Date.parse(String(raw)):NaN;return Number.isFinite(t)?t:Number(v?.id||0);};
const visitDateLabel=(v:any)=>{const raw=visitDateValue(v);if(!raw)return "Date/time not available";const d=new Date(raw);if(Number.isNaN(d.getTime()))return "Date/time not available";return d.toLocaleString("en-IN",{day:"2-digit",month:"short",year:"numeric",hour:"2-digit",minute:"2-digit",hour12:true});};

function Orders(){
 const search=useSearchParams();
 const [mode,setMode]=useState<Mode>(search.get("opdVisitId")?"CONSULTATION":"REGISTERED");
 const [patientSearch,setPatientSearch]=useState("");
 const [patients,setPatients]=useState<Patient[]>([]);
 const [patient,setPatient]=useState<Patient|null>(null);
 const [visitId,setVisitId]=useState(search.get("opdVisitId")||"");
 const [catalogue,setCatalogue]=useState<Investigation[]>([]);
 const [query,setQuery]=useState("");
 const [category,setCategory]=useState("ALL");
 const [selected,setSelected]=useState<Investigation[]>([]);
 const [existingOrderIds,setExistingOrderIds]=useState<number[]>([]);
 const [loading,setLoading]=useState(true);
 const [saving,setSaving]=useState(false);
 const [error,setError]=useState("");
 const [message,setMessage]=useState("");
 const [consultations,setConsultations]=useState<any[]>([]);
 const [consultationSearch,setConsultationSearch]=useState("");
 const [walkIn,setWalkIn]=useState({firstName:"",lastName:"",age:"",gender:"",phone:""});

 useEffect(()=>{Promise.all([fetch("/api/patients",{cache:"no-store"}).then(r=>r.ok?r.json():[]),fetch("/api/investigation-master",{cache:"no-store"}).then(r=>r.ok?r.json():[])]).then(([p,i])=>{setPatients(Array.isArray(p)?p:[]);setCatalogue(Array.isArray(i)?i:[]);}).catch(()=>setError("Unable to load patient or Investigation Master data.")).finally(()=>setLoading(false));},[]);
 useEffect(()=>{if(!visitId)return;fetch(`/api/investigation-orders?opdVisitId=${encodeURIComponent(visitId)}`,{cache:"no-store"}).then(r=>r.json()).then(rows=>{if(Array.isArray(rows)&&rows.length){const first=rows[0];setPatient(first.opdVisit?.patient||null);const items=rows.filter((x:any)=>x.status!=="CANCELLED").map((x:any)=>({id:x.investigationId,code:x.master?.code||"",name:x.investigation,category:x.master?.category||"",department:x.master?.department||null,rate:Number(x.netAmount||x.price||0),specimen:x.specimen||x.master?.specimen||null}));setSelected(items);setExistingOrderIds(rows.filter((x:any)=>x.status!=="CANCELLED").map((x:any)=>x.id));}}).catch(()=>{});},[visitId]);
 useEffect(()=>{if(mode!=="CONSULTATION")return;fetch("/api/investigation-orders/consultations",{cache:"no-store"}).then(r=>r.ok?r.json():[]).then(x=>setConsultations(Array.isArray(x)?x:[])).catch(()=>setConsultations([]));},[mode]);

 const patientOptions=useMemo(()=>{const q=patientSearch.toLowerCase().trim();return(q?patients.filter(p=>`${name(p)} ${p.patientId} ${p.phone||""}`.toLowerCase().includes(q)):patients).slice(0,20);},[patients,patientSearch]);
 const cats=useMemo(()=>Array.from(new Set(catalogue.map(x=>x.category).filter(Boolean))).sort(),[catalogue]);
 const options=useMemo(()=>{const q=query.toLowerCase().trim(),ids=new Set(selected.map(x=>x.id));return catalogue.filter(x=>!ids.has(x.id)).filter(x=>category==="ALL"||x.category===category).filter(x=>!q||`${x.name} ${x.code} ${x.category} ${x.department||""}`.toLowerCase().includes(q)).slice(0,40);},[catalogue,selected,category,query]);
 const consultationOptions=useMemo(()=>{const q=consultationSearch.toLowerCase().trim();return [...consultations].filter((v:any)=>{if(!q)return true;const p=v?.patient;const haystack=`${name(p||{firstName:"",lastName:""})} ${p?.patientId||""} ${p?.phone||""} ${v?.id||""} ${v?.tokenNumber||""} ${visitDateLabel(v)}`.toLowerCase();return haystack.includes(q);}).sort((a:any,b:any)=>visitTimestamp(b)-visitTimestamp(a));},[consultations,consultationSearch]);
 const total=selected.reduce((s,x)=>s+Number(x.rate||0),0);

 function resetPatientContext(){setPatient(null);setPatientSearch("");setVisitId("");setSelected([]);setExistingOrderIds([]);setWalkIn({firstName:"",lastName:"",age:"",gender:"",phone:""});}
 function selectMode(m:Mode){setMode(m);setError("");setMessage("");if(m!=="WALKIN")setWalkIn({firstName:"",lastName:"",age:"",gender:"",phone:""});if(m!=="CONSULTATION"&&m!=="REGISTERED")setVisitId("");if(m==="WALKIN")setPatient(null);}
 function choosePatient(p:Patient){setPatient(p);setPatientSearch("");setMessage("");setError("");}
 function continueWalkIn(){setError("");setMessage("");const first=walkIn.firstName.trim(),last=walkIn.lastName.trim(),age=Number(walkIn.age);if(!first||!last)return setError("First name and last name are required for a Walk-in / Outsider patient.");if(walkIn.age&&( !Number.isInteger(age)||age<0||age>120))return setError("Please enter a valid age between 0 and 120 years.");if(walkIn.phone&&walkIn.phone.length!==10)return setError("Please enter a valid 10-digit mobile number.");setPatient({id:0,patientId:"WALK-IN",firstName:first,lastName:last,gender:walkIn.gender||null,phone:walkIn.phone||null});setMessage("Walk-in patient information saved for this diagnostic order.");}
 function add(i:Investigation){setSelected(v=>[...v,i]);setMessage("");}
 async function remove(i:Investigation,index:number){if(existingOrderIds[index]){try{const r=await fetch(`/api/investigation-orders?orderId=${existingOrderIds[index]}`,{method:"DELETE"});const d=await r.json();if(!r.ok)throw new Error(d.error||"Unable to remove order.");setExistingOrderIds(v=>v.filter((_,n)=>n!==index));}catch(e){setError(e instanceof Error?e.message:"Unable to remove investigation.");return;}}setSelected(v=>v.filter((_,n)=>n!==index));}
 async function place(){setSaving(true);setError("");setMessage("");try{if(!patient)throw new Error("Complete patient information first.");if(!selected.length)throw new Error("Select at least one investigation.");const body:any={sourceType:mode,patientId:mode==="WALKIN"?undefined:patient.patientId,opdVisitId:visitId?Number(visitId):undefined,investigations:selected.map(x=>({id:x.id}))};if(mode==="WALKIN"){body.firstName=walkIn.firstName.trim();body.lastName=walkIn.lastName.trim();body.gender=walkIn.gender||undefined;body.phone=walkIn.phone||undefined;}const r=await fetch("/api/investigation-orders",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const d=await r.json();if(!r.ok)throw new Error(d.error||"Unable to place investigation order.");const pid=d.visit.patientId,vid=Number(d.visit.id);window.location.href=`/billing?patientId=${encodeURIComponent(pid)}&visitId=${vid}&from=investigation&bill=${encodeURIComponent(d.bill?.billNumber||"")}`;}catch(e){setError(e instanceof Error?e.message:"Unable to place investigation order.");}finally{setSaving(false);}}

 return <main className="min-h-screen bg-[#f5f8fc] text-slate-900">
  <header className="relative overflow-hidden bg-[#061a38] text-white"><div className="absolute -right-24 -top-32 h-80 w-80 rounded-full bg-blue-500/20 blur-3xl"/><div className="absolute -left-20 bottom-0 h-56 w-56 rounded-full bg-cyan-400/10 blur-3xl"/><div className="relative mx-auto max-w-7xl px-5 pb-8 pt-6 sm:px-8 lg:px-10"><div className="flex flex-col gap-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl border border-white/15 bg-white/10 text-sm font-black">S</div><div><p className="text-[9px] font-black uppercase tracking-[.3em] text-[#f1d27a]">SAMS · Diagnostics</p><p className="text-[10px] text-blue-200">Serawat Advanced Multispeciality Joint & Spine Centre</p></div></div><h1 className="mt-6 text-3xl font-black tracking-tight sm:text-4xl">Investigation Order</h1><p className="mt-2 max-w-2xl text-sm text-blue-100">Create a precise diagnostic order, review charges and move directly to the billing workflow.</p></div><div className="flex flex-wrap gap-2"><Link href="/reception" className="rounded-xl border border-white/15 bg-white/10 px-4 py-2.5 text-xs font-bold text-white">← Back</Link><Link href="/billing" className="rounded-xl border border-[#f1d27a]/40 bg-[#f1d27a]/10 px-4 py-2.5 text-xs font-black text-[#f1d27a]">Billing</Link><Link href="/investigation-room" className="rounded-xl border border-blue-300/25 bg-blue-500/10 px-4 py-2.5 text-xs font-bold text-blue-100">Investigation Room</Link></div></div><div className="mt-3 grid max-w-3xl grid-cols-3 gap-2 sm:gap-4"><Step n="01" label="Patient" active/><Step n="02" label="Investigations" active/><Step n="03" label="Order & Bill" active/></div></div></div></header>
  <div className="mx-auto max-w-7xl space-y-5 px-4 py-6 sm:px-8 lg:px-10">
   <section className="overflow-hidden rounded-[1.75rem] border border-slate-200 bg-white shadow-[0_14px_40px_rgba(15,35,65,.07)]"><div className="border-b border-slate-100 px-5 py-5 sm:px-7"><p className="text-[10px] font-black uppercase tracking-[.2em] text-blue-600">Step 01 · Order source</p><h2 className="mt-1 text-xl font-black text-[#082b61]">Who is this order for?</h2><p className="mt-1 text-xs text-slate-400">Registered, Visit, Walk-in and Referral orders use the same downstream investigation and billing workflow.</p><div className="mt-5 grid gap-2 md:grid-cols-4">{modes.map(([m,title,sub,num])=><button key={m} type="button" onClick={()=>selectMode(m)} className={`rounded-2xl border p-4 text-left transition-all ${mode===m?"border-blue-500 bg-blue-50 shadow-sm":"border-slate-200 bg-white hover:border-slate-300 hover:bg-slate-50"}`}><div className="flex items-center justify-between"><span className={`text-[10px] font-black ${mode===m?"text-blue-600":"text-slate-400"}`}>{num}</span><span className={`h-2 w-2 rounded-full ${mode===m?"bg-blue-500":"bg-slate-200"}`}/></div><p className={`mt-3 text-sm font-black ${mode===m?"text-[#082b61]":"text-slate-600"}`}>{title}</p><p className="mt-1 text-[10px] leading-4 text-slate-400">{sub}</p></button>)}</div></div>
    <div className="p-5 sm:p-7">
     {mode==="REGISTERED"&&<div className="rounded-2xl border border-slate-200 bg-slate-50/70 p-4 sm:p-5"><label className="mb-2 block text-[10px] font-black uppercase tracking-wider text-slate-500">Registered patient</label><div className="relative"><input value={patient?name(patient):patientSearch} onChange={e=>{if(patient){resetPatientContext();setPatientSearch(e.target.value);}else setPatientSearch(e.target.value)}} placeholder="Search by patient name, ID or mobile number…" className="w-full rounded-xl border border-slate-200 bg-white px-4 py-3.5 text-sm font-semibold outline-none focus:border-blue-500 focus:ring-4 focus:ring-blue-50"/>{!patient&&patientSearch&&<div className="absolute left-0 right-0 top-full z-20 mt-2 max-h-64 overflow-auto rounded-xl border border-slate-200 bg-white p-1 shadow-2xl">{patientOptions.map(p=><button key={p.id} type="button" onClick={()=>choosePatient(p)} className="block w-full rounded-lg border-b border-slate-100 px-4 py-3 text-left last:border-0 hover:bg-blue-50"><b className="text-sm text-[#082b61]">{name(p)}</b><span className="ml-2 text-xs text-slate-500">{p.patientId}{p.phone?` · ${p.phone}`:""}</span></button>)}</div>}</div>{patient&&<PatientCard patient={patient} onChange={resetPatientContext}/>}</div>}
     {mode==="CONSULTATION"&&<div><div className="mb-4 rounded-2xl border border-slate-200 bg-slate-50/70 p-4"><label className="mb-2 block text-[10px] font-black uppercase tracking-[.16em] text-slate-500">Search Visit</label><input value={consultationSearch} onChange={e=>setConsultationSearch(e.target.value)} placeholder="Search patient name, Patient ID, mobile, Visit #, token, date or time…" className="w-full rounded-xl border border-slate-200 bg-white px-4 py-3.5 text-sm font-semibold outline-none focus:border-blue-500 focus:ring-4 focus:ring-blue-50"/></div><div className="grid gap-2">{consultationOptions.length?consultationOptions.map((v:any)=><button key={v.id} type="button" onClick={()=>{setPatient(v.patient);setVisitId(String(v.id));setMessage("")}} className={`rounded-2xl border p-4 text-left transition ${visitId===String(v.id)?"border-blue-500 bg-blue-50 shadow-sm":"border-slate-200 bg-white hover:border-blue-200"}`}><div className="flex items-center justify-between gap-4"><div><b className="text-sm text-[#082b61]">{name(v.patient)}</b><span className="ml-2 text-xs text-slate-500">{v.patient?.patientId||"—"}</span><div className="mt-2 flex flex-wrap gap-2 text-[10px] text-slate-500"><span className="rounded-md bg-slate-100 px-2 py-1 font-bold">Visit #{v.id}</span><span className="rounded-md bg-slate-100 px-2 py-1 font-bold">Token #{v.tokenNumber??"—"}</span><span>{visitDateLabel(v)}</span></div></div><span className="rounded-lg bg-blue-600 px-3 py-1.5 text-[9px] font-black text-white">{visitId===String(v.id)?"SELECTED":"SELECT"}</span></div></button>):<div className="rounded-xl border border-dashed border-slate-200 p-7 text-center text-sm font-bold text-slate-500">No Visits found</div>}</div></div>}
     {mode==="WALKIN"&&<WalkInForm data={walkIn} setData={setWalkIn} patient={patient} onContinue={continueWalkIn} onChange={resetPatientContext}/>} 
     {mode==="EXTERNAL_REFERRAL"&&<div className="rounded-2xl border border-slate-200 bg-slate-50/70 p-5"><p className="text-sm font-black text-[#082b61]">External referral</p><p className="mt-1 text-xs text-slate-500">Enter patient details through the same diagnostic workflow. Patient name is required before investigations can be selected.</p><div className="mt-4 grid gap-4 sm:grid-cols-2"><input value={walkIn.firstName} onChange={e=>setWalkIn(v=>({...v,firstName:e.target.value}))} placeholder="First name" className="rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm"/><input value={walkIn.lastName} onChange={e=>setWalkIn(v=>({...v,lastName:e.target.value}))} placeholder="Last name" className="rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm"/><input value={walkIn.phone} onChange={e=>setWalkIn(v=>({...v,phone:e.target.value.replace(/\D/g,"").slice(0,10)}))} placeholder="Mobile number" inputMode="numeric" className="rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm"/><select value={walkIn.gender} onChange={e=>setWalkIn(v=>({...v,gender:e.target.value}))} className="rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm"><option value="">Gender</option><option value="male">Male</option><option value="female">Female</option><option value="other">Other</option></select></div><button type="button" onClick={continueWalkIn} className="mt-4 rounded-xl bg-[#082b61] px-5 py-3 text-sm font-black text-white">Continue with patient</button></div>}
     {patient&&<PatientCard patient={patient} age={mode==="WALKIN"?walkIn.age:undefined} onChange={resetPatientContext}/>}</div></section>

   <section className="rounded-[1.75rem] border border-slate-200 bg-white shadow-[0_14px_40px_rgba(15,35,65,.07)]"><div className="px-5 pt-6 sm:px-7"><p className="text-[10px] font-black uppercase tracking-[.2em] text-blue-600">Step 02 · Investigation selection</p><div className="mt-1 flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><h2 className="text-xl font-black text-[#082b61]">Choose investigations</h2><p className="mt-1 text-xs text-slate-400">This step is common for Registered Patient, Visit, Walk-in / Outsider and External Referral.</p></div>{selected.length>0&&<span className="rounded-full bg-blue-50 px-3 py-1.5 text-[10px] font-black text-blue-700">{selected.length} selected</span>}</div></div><div className="px-5 pb-6 sm:px-7"><div className="mt-5 grid gap-3 md:grid-cols-[1fr_190px]"><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search investigation, code or category…" className="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3.5 text-sm outline-none focus:border-blue-500 focus:bg-white focus:ring-4 focus:ring-blue-50"/><select value={category} onChange={e=>setCategory(e.target.value)} className="rounded-xl border border-slate-200 bg-slate-50 px-4 py-3.5 text-sm font-semibold outline-none focus:border-blue-500"><option value="ALL">All categories</option>{cats.map(c=><option key={c}>{c}</option>)}</select></div><div className="mt-4 grid gap-2 lg:grid-cols-2">{options.map(i=><button key={i.id} type="button" disabled={!patient} onClick={()=>add(i)} className="group rounded-2xl border border-slate-200 bg-white p-4 text-left transition hover:border-blue-300 hover:shadow-lg disabled:cursor-not-allowed disabled:opacity-45"><div className="flex items-center gap-3"><div className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-blue-50 text-xs font-black text-blue-600">+</div><div className="min-w-0 flex-1"><p className="truncate text-sm font-black text-[#082b61]">{i.name}</p><p className="mt-1 truncate text-[10px] text-slate-400">{i.code} · {i.category}{i.specimen?` · ${i.specimen}`:""}</p></div><span className="shrink-0 text-sm font-black text-[#0b63ce]">₹{Number(i.rate||0).toFixed(0)}</span></div></button>)}</div>{!patient&&<div className="mt-4 rounded-xl border border-amber-100 bg-amber-50 p-3 text-xs font-semibold text-amber-800">Complete Step 01 patient information before choosing investigations.</div>}{patient&&options.length===0&&<div className="mt-4 rounded-xl border border-dashed border-slate-200 p-6 text-center text-sm text-slate-400">No matching investigations found.</div>}</div></section>

   <section className="overflow-hidden rounded-[1.75rem] border border-slate-200 bg-white shadow-[0_14px_40px_rgba(15,35,65,.07)]"><div className="border-b border-slate-100 px-5 py-5 sm:px-7"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-center"><div><p className="text-[10px] font-black uppercase tracking-[.2em] text-blue-600">Step 03 · Final review</p><h2 className="mt-1 text-xl font-black text-[#082b61]">Investigation order</h2><p className="mt-1 text-xs text-slate-400">Review selected investigations, then continue to the common Billing workflow.</p></div>{selected.length>0&&<div className="rounded-2xl bg-[#082b61] px-4 py-3 text-right text-white"><p className="text-[9px] font-bold uppercase tracking-widest text-blue-200">Estimated total</p><p className="text-xl font-black">₹{total.toFixed(2)}</p></div>}</div></div><div className="p-5 sm:p-7">{selected.length===0?<div className="rounded-2xl border border-dashed border-slate-200 bg-slate-50/60 p-10 text-center"><p className="text-sm font-bold text-slate-500">No investigations selected</p><p className="mt-1 text-xs text-slate-400">Add investigations from Step 02 to build the order.</p></div>:<><div className="overflow-hidden rounded-2xl border border-slate-200">{selected.map((i,n)=><div key={`${i.id}-${n}`} className="grid gap-2 border-t border-slate-100 px-4 py-4 first:border-t-0 sm:grid-cols-[1fr_130px_110px_70px] sm:items-center"><div><p className="text-sm font-black text-[#082b61]">{i.name}</p><p className="mt-1 text-[10px] text-slate-400">{i.code} · {i.category}</p></div><span className="text-xs text-slate-500">{i.specimen||"—"}</span><p className="text-sm font-black sm:text-right">₹{Number(i.rate||0).toFixed(2)}</p><button type="button" onClick={()=>void remove(i,n)} className="justify-self-start rounded-lg px-2 py-1 text-[10px] font-black text-red-600 hover:bg-red-50 sm:justify-self-end">Remove</button></div>)}</div><div className="mt-5 flex flex-col gap-4 rounded-2xl bg-slate-50 p-4 sm:flex-row sm:items-center sm:justify-between sm:p-5"><div><p className="text-xs font-bold text-slate-500">{selected.length} investigation{selected.length===1?"":"s"} selected</p><p className="mt-1 text-[10px] text-slate-400">Billing, payment and receipt remain the same for all order sources.</p></div><button type="button" disabled={saving||!patient} onClick={()=>void place()} className="rounded-xl bg-gradient-to-r from-[#082b61] to-[#0b63ce] px-6 py-3.5 text-sm font-black text-white shadow-lg disabled:cursor-not-allowed disabled:opacity-50">{saving?"Creating Draft Bill…":"Place Order & Continue to Billing →"}</button></div></>}</div></section>
   {error&&<div className="rounded-2xl border border-red-200 bg-red-50 p-4 text-sm font-semibold text-red-700">{error}</div>}{message&&<div className="rounded-2xl border border-emerald-200 bg-emerald-50 p-4 text-sm font-semibold text-emerald-700">{message}</div>}
  </div>
 </main>;
}

function WalkInForm({data,setData,patient,onContinue,onChange}:{data:{firstName:string;lastName:string;age:string;gender:string;phone:string};setData:React.Dispatch<React.SetStateAction<{firstName:string;lastName:string;age:string;gender:string;phone:string}>>;patient:Patient|null;onContinue:()=>void;onChange:()=>void}){return <div className="rounded-2xl border border-blue-100 bg-gradient-to-br from-blue-50/80 to-white p-5 sm:p-6"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-[10px] font-black uppercase tracking-[.16em] text-blue-600">Walk-in / Outsider patient</p><h3 className="mt-1 text-lg font-black text-[#082b61]">Patient information</h3><p className="mt-1 text-xs text-slate-500">Enter the basic patient details. The next steps are identical to a Registered Patient order.</p></div>{patient&&<span className="rounded-full border border-emerald-200 bg-emerald-50 px-3 py-1.5 text-[10px] font-black text-emerald-700">✓ Information completed</span>}</div>{!patient?<><div className="mt-5 grid gap-4 sm:grid-cols-2"><Field label="First name *"><input autoFocus value={data.firstName} onChange={e=>setData(v=>({...v,firstName:e.target.value}))} placeholder="First name" className={inputClass}/></Field><Field label="Last name *"><input value={data.lastName} onChange={e=>setData(v=>({...v,lastName:e.target.value}))} placeholder="Last name" className={inputClass}/></Field><Field label="Age"><input value={data.age} onChange={e=>setData(v=>({...v,age:e.target.value.replace(/\D/g,"").slice(0,3)}))} inputMode="numeric" placeholder="Age in years" className={inputClass}/></Field><Field label="Gender"><select value={data.gender} onChange={e=>setData(v=>({...v,gender:e.target.value}))} className={inputClass}><option value="">Select gender</option><option value="male">Male</option><option value="female">Female</option><option value="other">Other</option><option value="prefer-not-to-say">Prefer not to say</option></select></Field><Field label="Mobile number" hint="Optional · 10 digits"><input value={data.phone} onChange={e=>setData(v=>({...v,phone:e.target.value.replace(/\D/g,"").slice(0,10)}))} inputMode="numeric" placeholder="10-digit mobile number" className={inputClass}/></Field></div><button type="button" onClick={onContinue} className="mt-5 rounded-xl bg-[#082b61] px-6 py-3 text-sm font-black text-white shadow-lg">Continue to Investigations →</button></>:<div className="mt-5 flex flex-col gap-3 rounded-2xl border border-emerald-100 bg-white p-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-sm font-black text-[#082b61]">{name(patient)}</p><p className="mt-1 text-xs text-slate-500">Age {data.age||"—"} years · {data.gender||"Gender not provided"}{data.phone?` · ${data.phone}`:""}</p><p className="mt-1 text-[10px] text-amber-600">Walk-in patient record will be created when the investigation order is placed.</p></div><button type="button" onClick={onChange} className="rounded-lg border border-slate-200 px-4 py-2 text-xs font-black text-slate-600">Edit</button></div>}</div>}
const inputClass="mt-2 w-full rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm font-semibold outline-none focus:border-blue-500 focus:ring-4 focus:ring-blue-50";
function Field({label,hint,children}:{label:string;hint?:string;children:React.ReactNode}){return <label className="block"><span className="flex justify-between gap-2 text-xs font-semibold text-slate-600"><span>{label}</span>{hint&&<span className="font-normal text-slate-400">{hint}</span>}</span>{children}</label>}
function PatientCard({patient,age,onChange}:{patient:Patient;age?:string;onChange:()=>void}){return <div className="mt-3 flex flex-col gap-3 rounded-2xl border border-emerald-100 bg-gradient-to-r from-emerald-50 to-white p-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-[9px] font-black uppercase tracking-widest text-emerald-700">Selected patient</p><p className="mt-0.5 text-sm font-black text-[#082b61]">{name(patient)} <span className="font-medium text-slate-400">· {patient.patientId}</span></p><p className="mt-1 text-[10px] text-slate-500">{age?`Age ${age} years · `:""}{patient.gender||"Gender not provided"}{patient.phone?` · ${patient.phone}`:""}</p></div><button type="button" onClick={onChange} className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-xs font-black text-slate-600">Change</button></div>}
function Step({n,label,active}:{n:string;label:string;active:boolean}){return <div className="flex items-center gap-2"><div className={`grid h-7 w-7 place-items-center rounded-full text-[9px] font-black ${active?"bg-white text-[#082b61]":"bg-white/10 text-blue-200"}`}>{n}</div><span className="text-[10px] font-black uppercase tracking-wider text-blue-100">{label}</span></div>}
export default function Page(){return <Suspense fallback={<main className="min-h-screen grid place-items-center bg-[#f5f8fc] font-bold text-slate-600">Loading Investigation Orders…</main>}><Orders/></Suspense>}
