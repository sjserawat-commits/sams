"use client";
import { useEffect,useState } from "react";
const roles=["ADMIN","DOCTOR","RECEPTION","BILLING","CLINICAL","LAB","PATIENT","SUPER_ADMIN"];
export default function Users(){const [users,setUsers]=useState<any[]>([]);const [form,setForm]=useState({username:"",displayName:"",password:"",role:"DOCTOR"});const [msg,setMsg]=useState("");async function load(){const r=await fetch("/api/admin/users");const d=await r.json();if(r.ok)setUsers(d);else setMsg(d.error||"Unable to load users")}useEffect(()=>{void load()},[]);async function add(e:React.FormEvent){e.preventDefault();setMsg("");const r=await fetch("/api/admin/users",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});const d=await r.json();if(!r.ok){setMsg(d.error);return}setForm({username:"",displayName:"",password:"",role:"DOCTOR"});setMsg("User created.");void load()}async function toggle(u:any){await fetch("/api/admin/users",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({id:u.id,active:!u.active})});void load()}return <main className="min-h-screen bg-[#f8f4ea] p-6 text-[#172b3a]"><div className="mx-auto max-w-6xl space-y-5"><header><p className="text-[10px] font-black tracking-[.3em] text-[#0d5b4c]">SAMS · ADMIN</p><h1 className="text-3xl font-black">Users & Roles</h1><p className="text-sm text-slate-500">Create, disable and assign least-privilege roles.</p></header><form onSubmit={add} className="grid gap-3 rounded-2xl bg-white p-5 shadow-sm md:grid-cols-5"><input required placeholder="Username" value={form.username} onChange={e=>setForm({...form,username:e.target.value})} className="rounded-xl border p-3"/><input required placeholder="Display name" value={form.displayName} onChange={e=>setForm({...form,displayName:e.target.value})} className="rounded-xl border p-3"/><input required minLength={12} type="password" placeholder="Password 12+" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} className="rounded-xl border p-3"/><select value={form.role} onChange={e=>setForm({...form,role:e.target.value})} className="rounded-xl border p-3">{roles.map(x=><option key={x}>{x}</option>)}</select><button className="rounded-xl bg-[#0d5b4c] p-3 font-black text-white">Create User</button></form>{msg&&<p className="rounded-xl bg-white p-3 text-sm font-semibold">{msg}</p>}<section className="overflow-hidden rounded-2xl bg-white shadow-sm"><table className="w-full text-left text-sm"><thead className="bg-[#fbf7ed] text-xs uppercase"><tr><th className="p-4">User</th><th>Role</th><th>Status</th><th></th></tr></thead><tbody>{users.map(u=><tr key={u.id} className="border-t"><td className="p-4"><b>{u.displayName}</b><div className="text-xs text-slate-400">{u.username}</div></td><td>{u.role}</td><td>{u.active?"Active":"Disabled"}</td><td className="pr-4 text-right"><button onClick={()=>void toggle(u)} className="rounded-lg border px-3 py-2 text-xs font-bold">{u.active?"Disable":"Enable"}</button></td></tr>)}</tbody></table></section></div></main>}
