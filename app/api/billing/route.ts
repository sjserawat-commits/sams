import { NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";
const round=(v:number)=>Math.round(v*100)/100;
const billNumber=()=>`SAMS-${Date.now().toString().slice(-10)}`;
const receiptNumber=()=>`RCP-${Date.now().toString().slice(-10)}`;
const CONSULTATION_FEE=500;
async function syncVisitCharges(visitId:number){const visit=await prisma.oPDVisit.findUnique({where:{id:visitId},include:{patient:true,departmentMaster:true,clinicalEncounter:true,investigationOrders:{orderBy:{createdAt:"asc"}},prescriptions:{where:{status:"ACTIVE"},orderBy:{createdAt:"asc"}},billingRecords:{orderBy:{createdAt:"desc"}},billingLineItems:{orderBy:{createdAt:"asc"}}}});if(!visit)return null;const bill=visit.billingRecords[0];const locked=Boolean(bill?.paidAmount)||bill?.paymentStatus==="PAID";for(const order of visit.investigationOrders){const existing=visit.billingLineItems.find(l=>l.sourceType==="INVESTIGATION_ORDER"&&l.sourceId===order.id);if(!existing&&!locked)await prisma.billingLineItem.create({data:{billingRecordId:bill?.id??undefined,opdVisitId:visit.id,serviceType:"INVESTIGATION",description:order.investigation,quantity:1,unitPrice:round(order.netAmount),amount:round(order.netAmount),sourceType:"INVESTIGATION_ORDER",sourceId:order.id}});}for(const prescription of visit.prescriptions){const existing=visit.billingLineItems.find(l=>l.sourceType==="PRESCRIPTION"&&l.sourceId===prescription.id);if(!existing&&!locked)await prisma.billingLineItem.create({data:{billingRecordId:bill?.id??undefined,opdVisitId:visit.id,serviceType:"PHARMACY",description:prescription.medicineName,quantity:prescription.quantity,unitPrice:round(prescription.unitPrice),amount:round(prescription.quantity*prescription.unitPrice),sourceType:"PRESCRIPTION",sourceId:prescription.id}});}const refreshed=await prisma.oPDVisit.findUnique({where:{id:visitId},include:{patient:true,departmentMaster:true,clinicalEncounter:true,billingLineItems:{orderBy:{createdAt:"asc"}},billingRecords:{orderBy:{createdAt:"desc"}}}});if(!refreshed)return null;const latest=refreshed.billingRecords[0];if(latest&&latest.paidAmount===0&&latest.paymentStatus!=="DISCARDED"){const subtotal=round(refreshed.billingLineItems.reduce((s,l)=>s+l.amount,0));const net=round(Math.max(0,subtotal-latest.discount));await prisma.billingRecord.update({where:{id:latest.id},data:{subtotal,netAmount:net,balanceAmount:net}});refreshed.billingRecords[0]={...latest,subtotal,netAmount:net,balanceAmount:net};}return refreshed;}
export async function GET(request:Request){try{const p=new URL(request.url).searchParams,patientId=p.get("patientId")?.trim()||"",visitIdRaw=p.get("visitId")?.trim()||"",visitId=visitIdRaw?Number(visitIdRaw):null;if(patientId&&visitId===null){const patient=await prisma.patient.findUnique({where:{patientId}});if(!patient)return NextResponse.json({error:"Patient not found."},{status:404});const visits=await prisma.oPDVisit.findMany({where:{patientId:patient.id},include:{departmentMaster:true},orderBy:{createdAt:"desc"}});return NextResponse.json({patient:{id:patient.id,patientId:patient.patientId,name:`${patient.firstName} ${patient.lastName}`.trim(),phone:patient.phone},visits:visits.map(v=>({id:v.id,tokenNumber:v.tokenNumber,visitType:v.visitType,status:v.status,department:v.departmentMaster?.name??null,createdAt:v.createdAt}))});}if(visitId===null||!Number.isInteger(visitId)||visitId<=0)return NextResponse.json({error:"A valid OPD visit ID or patient ID is required."},{status:400});const visit=await syncVisitCharges(visitId);if(!visit)return NextResponse.json({error:"OPD visit not found."},{status:404});const lineItems=visit.billingLineItems.map(l=>({id:l.id,serviceType:l.serviceType,description:l.description,quantity:l.quantity,unitPrice:round(l.unitPrice),amount:round(l.amount),sourceType:l.sourceType,sourceId:l.sourceId}));return NextResponse.json({visit:{id:visit.id,tokenNumber:visit.tokenNumber,visitType:visit.visitType,status:visit.status,department:visit.departmentMaster?.name??null,createdAt:visit.createdAt},patient:{id:visit.patient.id,patientId:visit.patient.patientId,name:`${visit.patient.firstName} ${visit.patient.lastName}`.trim(),phone:visit.patient.phone},encounter:visit.clinicalEncounter?{id:visit.clinicalEncounter.id,treatmentPlan:visit.clinicalEncounter.treatmentPlan,followUpDate:visit.clinicalEncounter.followUpDate}:null,lineItems,totals:{subtotal:round(lineItems.reduce((s,l)=>s+l.amount,0))},bill:visit.billingRecords[0]??null,consultationFee:CONSULTATION_FEE});}catch(e){console.error(e);return NextResponse.json({error:"Unable to load billing information."},{status:500});}}
export async function POST(request:Request){try{const body=await request.json(),visitId=Number(body?.visitId);if(!Number.isInteger(visitId)||visitId<=0)return NextResponse.json({error:"A valid OPD visit ID is required."},{status:400});const visit=await syncVisitCharges(visitId);if(!visit)return NextResponse.json({error:"OPD visit not found."},{status:404});const bill=visit.billingRecords[0];
if(body?.action==="addLine"){const serviceType=String(body.serviceType??"OTHER").trim().toUpperCase(),description=String(body.description??"").trim(),quantity=Number(body.quantity),unitPrice=Number(body.unitPrice);if(!description||!Number.isFinite(quantity)||quantity<=0||!Number.isFinite(unitPrice)||unitPrice<0)return NextResponse.json({error:"Enter service, quantity and valid rate."},{status:400});if(bill?.paidAmount||bill?.paymentStatus==="PAID")return NextResponse.json({error:"Paid bills cannot be modified."},{status:400});const amount=round(quantity*unitPrice);const line=await prisma.billingLineItem.create({data:{billingRecordId:bill?.id??undefined,opdVisitId:visitId,serviceType,description,quantity,unitPrice:round(unitPrice),amount}});if(bill)await prisma.billingRecord.update({where:{id:bill.id},data:{subtotal:round(bill.subtotal+amount),netAmount:round(Math.max(0,bill.netAmount+amount)),balanceAmount:round(Math.max(0,bill.balanceAmount+amount))}});return NextResponse.json({line},{status:201});}
if(body?.action==="addConsultation"){if(!bill)return NextResponse.json({error:"No draft bill exists for this Visit."},{status:404});if(bill.paidAmount||bill.paymentStatus==="PAID")return NextResponse.json({error:"Paid bills cannot be modified."},{status:400});const exists=await prisma.billingLineItem.findFirst({where:{billingRecordId:bill.id,serviceType:"CONSULTATION",sourceType:"OPD_VISIT"}});if(exists)return NextResponse.json({message:"Consultation fee is already included.",line:exists});const line=await prisma.billingLineItem.create({data:{billingRecordId:bill.id,opdVisitId:visitId,serviceType:"CONSULTATION",description:"OPD Consultation Fee",quantity:1,unitPrice:CONSULTATION_FEE,amount:CONSULTATION_FEE,sourceType:"OPD_VISIT",sourceId:visitId}});const updated=await prisma.billingRecord.update({where:{id:bill.id},data:{subtotal:round(bill.subtotal+CONSULTATION_FEE),netAmount:round(Math.max(0,bill.netAmount+CONSULTATION_FEE)),balanceAmount:round(Math.max(0,bill.balanceAmount+CONSULTATION_FEE))}});return NextResponse.json({line,bill:updated},{status:201});}
if(body?.action==="processBill"){if(!bill)return NextResponse.json({error:"No draft bill exists for this Visit."},{status:404});if(bill.paymentStatus==="DISCARDED")return NextResponse.json({error:"This bill has already been discarded."},{status:409});if(bill.paidAmount>0)return NextResponse.json({bill,existing:true});const lines=await prisma.billingLineItem.findMany({where:{billingRecordId:bill.id}});const subtotal=round(lines.reduce((s,l)=>s+l.amount,0)),net=round(Math.max(0,subtotal-bill.discount));const updated=await prisma.billingRecord.update({where:{id:bill.id},data:{subtotal,netAmount:net,balanceAmount:net,paymentStatus:"UNPAID"}});return NextResponse.json({bill:updated,existing:true,processed:true});}
const subtotal=round(visit.billingLineItems.reduce((s,l)=>s+l.amount,0));if(bill)return NextResponse.json({bill,existing:true});const created=await prisma.billingRecord.create({data:{billNumber:billNumber(),patientId:visit.patientId,opdVisitId:visit.id,subtotal,netAmount:subtotal,balanceAmount:subtotal,paymentStatus:"UNPAID"}});await prisma.billingLineItem.updateMany({where:{opdVisitId:visitId,billingRecordId:null},data:{billingRecordId:created.id}});return NextResponse.json({bill:created,existing:false},{status:201});}catch(e){console.error(e);return NextResponse.json({error:e instanceof Error?e.message:"Unable to update billing."},{status:500});}}
export async function PATCH(request:Request){try{const body=await request.json(),id=Number(body?.billId);if(!Number.isInteger(id)||id<=0)return NextResponse.json({error:"A valid bill ID is required."},{status:400});const bill=await prisma.billingRecord.findUnique({where:{id}});if(!bill)return NextResponse.json({error:"Bill not found."},{status:404});if(body?.action==="discardBill"){if(bill.paidAmount||bill.paymentStatus==="PAID")return NextResponse.json({error:"A paid bill cannot be discarded."},{status:400});const updated=await prisma.$transaction(async tx=>{const source=await tx.billingLineItem.findMany({where:{billingRecordId:bill.id,sourceType:"INVESTIGATION_ORDER",sourceId:{not:null}},select:{sourceId:true}});await tx.billingLineItem.deleteMany({where:{billingRecordId:bill.id}});const ids=source.map(x=>x.sourceId).filter((x):x is number=>typeof x==="number");if(ids.length)await tx.investigationOrder.updateMany({where:{id:{in:ids}},data:{status:"CANCELLED",paymentStatus:"UNPAID"}});return tx.billingRecord.update({where:{id:bill.id},data:{paymentStatus:"DISCARDED",balanceAmount:0}});});return NextResponse.json({bill:updated,discarded:true});}if(body?.action==="discount"){if(bill.paidAmount)return NextResponse.json({error:"Paid bills cannot be discounted."},{status:400});const discount=Number(body.discount);if(!Number.isFinite(discount)||discount<0||discount>bill.subtotal)return NextResponse.json({error:"Enter a valid discount."},{status:400});const net=round(bill.subtotal-discount);return NextResponse.json({bill:await prisma.billingRecord.update({where:{id},data:{discount:round(discount),netAmount:net,balanceAmount:net}})});}const paid=Number(body?.paidAmount),method=String(body?.paymentMethod??"").trim().toUpperCase();if(!Number.isFinite(paid)||paid<=0||!method)return NextResponse.json({error:"Bill, valid payment amount and payment method are required."},{status:400});if(paid>bill.balanceAmount)return NextResponse.json({error:"Payment exceeds outstanding balance."},{status:400});const newPaid=round(bill.paidAmount+paid),balance=round(Math.max(0,bill.netAmount-newPaid));const updated=await prisma.billingRecord.update({where:{id},data:{paidAmount:newPaid,balanceAmount:balance,paymentMethod:method,paymentStatus:balance===0?"PAID":"PARTIAL",paidAt:new Date(),receiptNumber:bill.receiptNumber??receiptNumber()}});
// Payment status propagates only to investigation orders actually included on this bill.
const billedInvestigationIds=await prisma.billingLineItem.findMany({where:{billingRecordId:bill.id,sourceType:"INVESTIGATION_ORDER",sourceId:{not:null}},select:{sourceId:true}});const investigationIds=billedInvestigationIds.map(x=>x.sourceId).filter((x):x is number=>typeof x==="number");if(investigationIds.length){await prisma.investigationOrder.updateMany({where:{id:{in:investigationIds}},data:{paymentStatus:balance===0?"PAID":"PARTIAL"}});if(balance===0){await prisma.investigationOrder.updateMany({where:{id:{in:investigationIds},status:"ORDERED"},data:{status:"APPROVED_FOR_SAMPLING"}});}}return NextResponse.json({bill:updated});}catch(e){console.error(e);return NextResponse.json({error:e instanceof Error?e.message:"Unable to update billing."},{status:500});}}
export async function DELETE(request:Request){try{const id=Number(new URL(request.url).searchParams.get("lineId"));if(!Number.isInteger(id)||id<=0)return NextResponse.json({error:"A valid line item ID is required."},{status:400});const line=await prisma.billingLineItem.findUnique({where:{id},include:{billingRecord:true}});if(!line)return NextResponse.json({error:"Charge not found."},{status:404});if(line.billingRecord?.paidAmount||line.billingRecord?.paymentStatus==="PAID")return NextResponse.json({error:"Paid bills cannot be modified."},{status:400});await prisma.billingLineItem.delete({where:{id}});if(line.billingRecordId){const remaining=await prisma.billingLineItem.findMany({where:{billingRecordId:line.billingRecordId}});const subtotal=round(remaining.reduce((s,x)=>s+x.amount,0));await prisma.billingRecord.update({where:{id:line.billingRecordId},data:{subtotal,netAmount:round(Math.max(0,subtotal-line.billingRecord!.discount)),balanceAmount:round(Math.max(0,subtotal-line.billingRecord!.discount-line.billingRecord!.paidAmount))}});}return NextResponse.json({ok:true});}catch(e){console.error(e);return NextResponse.json({error:"Unable to remove charge."},{status:500});}}
