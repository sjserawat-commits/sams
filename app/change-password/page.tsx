"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export default function ChangePassword(){const r=useRouter();const[current,setCurrent]=useState(""),[next,setNext]=useState(""),[confirm,setConfirm]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setError("");if(next!==confirm)return setError("New passwords do not match.");setBusy(true);try{const x=await fetch("/api/auth/change-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({currentPassword:current,newPassword:next})});const d=await x.json();if(!x.ok)throw new Error(d.error);r.replace("/dashboard")}catch(e){setError(e instanceof Error?e.message:"Unable to change password.")}finally{setBusy(false)}}return <main className="grid min-h-screen place-items-center bg-[#080d16] p-5 text-slate-100"><form onSubmit={submit} className="w-full max-w-md rounded-[2rem] border border-white/10 bg-[#101925] p-8 shadow-2xl"><p className="text-[9px] font-black uppercase tracking-[.3em] text-[#d9bb72]">SAMS · SECURITY</p><h1 className="mt-2 text-3xl font-black">Set your password</h1><p className="mt-2 text-sm leading-6 text-slate-400">Your administrator issued a temporary password. Choose a private password before continuing.</p><div className="mt-7 space-y-3"><input required type="password" value={current} onChange={e=>setCurrent(e.target.value)} placeholder="Temporary / current password" className="w-full rounded-xl border border-white/10 bg-[#0b1420] p-3"/><input required minLength={12} type="password" value={next} onChange={e=>setNext(e.target.value)} placeholder="New password (12+ characters)" className="w-full rounded-xl border border-white/10 bg-[#0b1420] p-3"/><input required minLength={12} type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="Confirm new password" className="w-full rounded-xl border border-white/10 bg-[#0b1420] p-3"/>{error&&<p className="rounded-xl bg-red-400/10 p-3 text-sm font-bold text-red-300">{error}</p>}<button disabled={busy} className="w-full rounded-xl bg-[#c9a85c] p-3 text-xs font-black text-[#10151d]">{busy?"Saving…":"Set Password & Continue"}</button></div></form></main>}
